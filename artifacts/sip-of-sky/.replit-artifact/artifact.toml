kind = "web"
previewPath = "/"
title = "Sip of Sky"
version = "1.0.0"
id = "artifacts/sip-of-sky"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22240

[services.development]
run = "pnpm --filter @workspace/sip-of-sky run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/sip-of-sky", "run", "build" ]
publicDir = "artifacts/sip-of-sky/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22240"
BASE_PATH = "/"
